#
# Currently the Android plugin for Netbeans does not support local.properties file and has a bug which prevents the usage of GUI platform customization. It is required to MANUALLY set your platfom HERE by editing the following property "platform.active" or else Netbeans might crash.
platform.active=UnsetAndroidPlatform
#
# It is required to update the location of xmlvm, if the project changes location
xmlvm.sdk.jar=__XMLVMSDK__
#
assets.dir=
application.title=__PROJNAME__
application.vendor=${user.name}
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java
# This directory is removed when the project is cleaned:
build.dir=bin
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
dist.apk=${dist.dir}/__PROJNAME__.apk
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
includes=**
intermediate.dex=${build.dir}/classes.dex
javac.classpath=
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.component=my.xtest
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
resource.dir=res
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
src.gen.dir=gen
